fastapi
uvicorn
# numpy MUST stay <2: the dlib 19.24.1 wheel is built against the numpy 1.x C ABI.
# With numpy>=2 every face_recognition call raises
# "Unsupported image type, must be 8bit gray or RGB image."
numpy==1.26.4
# opencv 5.x hard-requires numpy>=2, so it is pinned to the 4.x line to match.
# Install ONLY this opencv variant. opencv-python / opencv-contrib-python unpack
# into the same cv2/ directory and will silently overwrite each other.
opencv-python-headless==4.10.0.84

# Face pipeline (routers/enroll.py, routers/iris.py). On Windows dlib must come
# from the bundled cp311 wheel -- PyPI ships no Windows binary, so a plain
# `pip install dlib` tries to build from source and needs CMake + MSVC.
# Listed before face_recognition so its dlib dependency is already satisfied.
./dlib-19.24.1-cp311-cp311-win_amd64.whl ; platform_system == "Windows"
dlib==19.24.1 ; platform_system != "Windows"
face_recognition==1.3.0
pillow

firebase-admin
twilio
python-dotenv
python-multipart
brevo-python