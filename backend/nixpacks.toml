[phases.setup]
nixPkgs = ["python311", "cmake", "gcc", "g++", "pkg-config"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"